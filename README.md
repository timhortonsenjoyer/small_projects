# small_projects
Projects coded in either C++ or Python. These projects include: 
1. Terminal-based calculator. Takes a string input of all the operations expected to be performed, and outputs result e.g. 5+3+2-1 would give an output of 9. Currently only supports +,-,* and / operators with int types. The future iteration of this project will be to add GUI capabilities
